" Markdown Previewer
Plug 'vinnymeller/swagger-preview.nvim', { 'do': 'npm install swagger-ui-watcher -g' }

function! SetupSwaggerPreview()
  echom "SetupCmp billy idol"
lua << EOF
require("swagger-preview").setup({
    port = 9654,
    host = "localhost",
})
EOF
endfunction

autocmd VimEnter * call SetupSwaggerPreview()
